{
  "name": "tech-analytica-task-management",
  "version": "1.0.0",
  "private": true,
  "description": "Task Management System with RBAC and Audit Logging",
  "author": "Anik Deb",
  "license": "MIT",
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev": "pnpm run dev:backend & pnpm run dev:frontend",
    "build": "pnpm --filter shared-types build && pnpm --filter backend build && pnpm --filter frontend build",
    "db:migrate": "pnpm --filter backend prisma:migrate",
    "db:seed": "pnpm --filter backend prisma:seed",
    "db:studio": "pnpm --filter backend prisma:studio"
  }
}
